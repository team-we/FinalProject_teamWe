<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.gmail.whwan09.service.MemberDao">
		<resultMap type="com.gmail.whwan09.entities.Member" id="memberMap">
			<id property="id"    column="id" />
			<id property="password" column="password" />
			<id property="name"     column="name" />
		</resultMap>
		
		<select id="selectCount" parameterType="String" resultType="int">
			select count(*) from member where id=#{id}
		</select>
		
		<select id="selectOne" parameterType="String" resultMap="memberMap">
			select * from member where id=#{id}
		</select>
		
		<select id="selectAll" resultMap="memberMap">
			select * from member
		</select>
		
		<insert id="insertRow" parameterType="hashMap">
			insert into member (id,password,name)
			values
			(#{id},#{password},#{name})
		</insert>
		
		<update id="updateRow" parameterType="hashMap">
			update member set id=#{id},password=#{password},name=#{name} 
			where id=#{id}
		</update>
		
		<delete id="deleteRow" parameterType="String">
			delete from member 
			where id=#{id}
		</delete>
		
		<select id="selectLogin" parameterType="hashMap" resultMap="memberMap">
			select * from member where id=#{id} and password=#{password}
		</select>
	</mapper>